# Candidate area

No candidate is admitted at initialization. Historical mutable integration
branches must be transformed into self-contained, manifest-complete candidates;
they are not imported or merged here wholesale.

Three namespaces are now reserved, but no overlay is admitted:

- `commons/stacks/fac` → `candidates/commons/stacks/fac`
- `commons/stacks/tohoku` → `candidates/commons/stacks/tohoku`
- `commons/stacks/gaga` → `candidates/commons/stacks/gaga`

All three are leased to task `019fca5a-c29e-7330-acdc-c93f4a3dc9fb` against
the exact upstream Stacks lock. Read `CONTRACT.md` before adding material.
